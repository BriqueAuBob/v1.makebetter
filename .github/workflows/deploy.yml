name: Deployment to server
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: rjstone/discord-webhook-notify@v1
        with:
          severity: info
          details: Deployment started...
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - run: |
          cd ..
          rm -rf makebetter-frontend_new
          cp -r makebetter-frontend makebetter-frontend_new
          cd makebetter-frontend_new
          git checkout main
          git reset --hard origin/main
          git pull
          npm rebuild
          npm install

          # Build the api

          npm run build

          # Replace current version with the new one

          cd ../
          mv makebetter-frontend makebetter-frontend_old
          mv makebetter-frontend_new makebetter-frontend

          # Restart server

          cd makebetter-frontend
          pm2 delete makebetter-frontend
          pm2 start .output/server/index.mjs --name makebetter-frontend
          rm -rf ../makebetter-frontend_old
      - uses: rjstone/discord-webhook-notify@v1
        if: success()
        with:
          severity: info
          details: Nouvelle version déployée avec succès
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
      - uses: rjstone/discord-webhook-notify@v1
        if: failure()
        with:
          severity: error
          details: Something went wrong...
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
      - uses: rjstone/discord-webhook-notify@v1
        if: cancelled()
        with:
          severity: warn
          details: Deploy cancelled...
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
